
pixoo:
  host: 'localhost'
  health-forward: true

server:
  shutdown: 'graceful'

spring:
  lifecycle:
    timeout-per-shutdown-phase: 1m
  jackson:
    deserialization:
      fail-on-unknown-properties: true
